{
  "name": "sharma-event-management",
  "version": "1.0.0",
  "private": true,
  "description": "Premium Event Management Platform - Sharma Events, Haldwani",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev",
    "postinstall": "npm run install:all && npm run build",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node start.mjs",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
